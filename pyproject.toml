[tool.poetry]
package-mode = false

[tool.poetry.dependencies]
python = "^3.13"
pygame = "^2.6.0"
flake8 = "^6.0.0"
mypy = "^1.0.0"

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"